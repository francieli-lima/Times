import 'package:flutter/material.dart';

class TimesDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Color(0x7C000000),
      thickness: 0.1,
    );
  }
}
